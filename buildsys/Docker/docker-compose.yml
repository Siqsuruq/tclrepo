services:
  ubuntu-builder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bawt_glibc231_builder
    tty: true
    stdin_open: true
    volumes:
      - /opt/bawt:/workspace
    working_dir: /workspace
    environment:
      - LC_ALL=C.UTF-8
      - LANG=C.UTF-8